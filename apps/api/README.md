# apps/api — KSC Public Record Intelligence API

FastAPI service. Phase 4 exposes only system endpoints (`/health`, `/ready`, `/version`)
and the minimal data model (`cases`, `documents`, `audit_log`).

See `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md` and `docs/DEVELOPMENT.md` at the repo root.
