{
  "name": "@ksc/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contract types and vocabularies (HANDOFF.md §9) for KSC Public Record Intelligence",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"(shared) linted via apps/web eslint\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"(shared) no tests\""
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
