# packages/prompts

Reserved for versioned prompt templates used by the (future) analysis layer.

Empty in Phase 4 — no AI integration exists yet. When prompts are added they
must encode the neutrality and citation rules in `CLAUDE.md` (no guilt,
suspicion, credibility or success scores; `UNRESOLVED` citations are never
replaced; protected witnesses stay as their W-code).
